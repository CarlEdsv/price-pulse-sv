import { defineConfig } from 'vite'
// ... otros imports si tienes (como react)

export default defineConfig({
  base: '/price-pulse-sv/', // ESTA ES LA CLAVE: debe coincidir con el nombre de tu repositorio
  // ... el resto de tu configuración
})
